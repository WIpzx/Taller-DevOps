# ============================================================
# Credenciales LOCALES de DEMO para el entorno DEV.
# NO son secretos reales. Cámbialas antes de cualquier uso fuera de local.
#
# Este archivo (.env.dev.example) SÍ se versiona como plantilla.
# El archivo real ".env.dev" lo genera el script start-dev.sh y está en .gitignore.
#
# Uso:  docker compose --env-file .env.dev -f docker-compose.dev.yml up -d --build
# ============================================================
MYSQLDB_USER=root
MYSQLDB_ROOT_PASSWORD=changeme_dev_local

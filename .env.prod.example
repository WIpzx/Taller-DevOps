# ============================================================
# Credenciales LOCALES de DEMO para el entorno PROD (simulado, local).
# NO son secretos reales. Cámbialas antes de cualquier uso fuera de local.
#
# Este archivo (.env.prod.example) SÍ se versiona como plantilla.
# El archivo real ".env.prod" lo genera el script start-prod.sh y está en .gitignore.
#
# Uso:  docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build
# ============================================================
MYSQLDB_USER=root
MYSQLDB_ROOT_PASSWORD=changeme_prod_local
